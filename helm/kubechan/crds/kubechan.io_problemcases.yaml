---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: problemcases.kubechan.io
spec:
  group: kubechan.io
  names:
    kind: ProblemCase
    listKind: ProblemCaseList
    plural: problemcases
    singular: problemcase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.severity
      name: Severity
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.detector
      name: Detector
      type: string
    - jsonPath: .spec.affectedResource.name
      name: Resource
      type: string
    - jsonPath: .status.firstSeen
      name: First Seen
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ProblemCase is the Schema for the problemcases API.
          It represents a detected problem in the Kubernetes cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProblemCaseSpec defines the desired state of ProblemCase.
            properties:
              affectedResource:
                description: AffectedResource is the primary Kubernetes resource that
                  triggered this problem.
                properties:
                  kind:
                    description: Kind is the Kubernetes resource kind (e.g., Pod,
                      Deployment).
                    type: string
                  name:
                    description: Name is the resource name.
                    type: string
                  namespace:
                    description: Namespace of the resource. Empty for cluster-scoped
                      resources.
                    type: string
                required:
                - kind
                - name
                type: object
              detector:
                description: Detector is the name of the detector that identified
                  this problem.
                type: string
              relatedResources:
                description: RelatedResources is a list of additional Kubernetes resources
                  relevant to this problem.
                items:
                  description: ResourceRef identifies a Kubernetes resource by namespace,
                    kind, and name.
                  properties:
                    kind:
                      description: Kind is the Kubernetes resource kind (e.g., Pod,
                        Deployment).
                      type: string
                    name:
                      description: Name is the resource name.
                      type: string
                    namespace:
                      description: Namespace of the resource. Empty for cluster-scoped
                        resources.
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              severity:
                description: Severity is the assessed severity of this problem.
                enum:
                - critical
                - high
                - medium
                - low
                type: string
              symptoms:
                description: Symptoms is a list of human-readable descriptions of
                  the observed symptoms.
                items:
                  type: string
                type: array
            required:
            - affectedResource
            - detector
            - severity
            type: object
          status:
            description: ProblemCaseStatus defines the observed state of ProblemCase.
            properties:
              firstSeen:
                description: FirstSeen is when this problem was first detected.
                format: date-time
                type: string
              lastSeen:
                description: LastSeen is when symptoms were last observed for this
                  problem.
                format: date-time
                type: string
              latestAnalysisResultRef:
                description: LatestAnalysisResultRef is the SQLite analysis_results.id
                  of the most recent analysis.
                type: string
              latestDiagnosticRunRef:
                description: LatestDiagnosticRunRef is the name of the most recently
                  created DiagnosticRun CRD.
                type: string
              resolvedAt:
                description: ResolvedAt is when this problem was resolved.
                format: date-time
                type: string
              state:
                default: open
                description: State is the current lifecycle state of this problem.
                enum:
                - open
                - investigating
                - resolved
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
